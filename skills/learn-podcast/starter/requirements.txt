# Learn Podcast — runtime deps.
#
# Core (always required)
feedparser>=6.0
requests>=2.31
PyYAML>=6.0
trafilatura>=1.6
edge-tts>=6.1

# Provider SDK — install only the one set in config.yaml → llm.provider
# anthropic>=0.40        # if provider: anthropic
# google-genai>=0.3      # if provider: gemini
# openai>=1.50           # if provider: openai

# Optional extras — install only the ones your config uses:
#
# Podcast feed hosting on S3-compatible storage (Cloudflare R2, AWS S3, B2):
# boto3>=1.34
#
# LinkedIn personal-profile posts via Apify harvestapi actor:
# apify-client>=1.7
#
# Pending-sources watcher (fuzzy match on podcast launches):
# rapidfuzz>=3.0
#
# Monthly Exa source-discovery expand.py:
# exa-py>=1.0
# openai>=1.50           # for text-embedding-3-small re-scoring
# numpy>=1.26
